output: dist/
releases:
  - name: windows-portable
    jobs:
      - name: windows-exe
        package:
          platform: windows
          target: exe
          build_args:
            release: true
            
  - name: windows-installer
    jobs:
      - name: windows-inno
        package:
          platform: windows
          target: inno
          build_args:
            release: true